name: Changeset Release (Manual Only)

on:
    workflow_dispatch:

jobs:
    disabled-by-default:
        runs-on: ubuntu-latest
        steps:
            - name: Info
              run: |
                  echo "Automatic changeset release is disabled on this fork."
                  echo "Use .github/workflows/marketplace-publish.yml for internal asset builds on main-vertex."
