/** Reported when Git cannot be spawned because the binary is missing from PATH. */
export const MISSING_GIT =
  "Git is not installed or not found in PATH. Please install Git (https://git-scm.com) and restart VS Code."
